# Hyprland

Standalone Home Manager Hyprland configuration.

## Layout

- `flake.nix`: standalone Home Manager flake
- `config`: Hyprland module tree

## Commands

```sh
nix flake check
home-manager switch --flake .#x86_64-linux
```
